CREATE TABLE "Y24GROUP054"."CLASS" (
    "CLASS_ID" INTEGER GENERATED BY DEFAULT AS IDENTITY (
        START WITH 1
    ) CONSTRAINT CLASS_ID_NN NOT NULL ENABLE,
    "CLASS_NAME" VARCHAR(5),
    "ROOM_ID" INTEGER NOT NULL
);